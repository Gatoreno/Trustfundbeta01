<script type="text/javascript" src="https://ajax.googleapis.com/ajax/libs/jquery/1.11.0/jquery.min.js"></script>


{{#if user.id_client}}

<div class="row">
    <div class="col-lg-12">


        <div>


            <label for="">unidades usadas
                <input type="text" id="unitsUsed">
            </label>

            <label for="">proyectos apoyados
                <input type="text" id="nprojects">
            </label>

            <label for="">unidades compradas 
                <input type="text" id="unitsbought">
            </label>

            <label for="">subscripciones 
                <input type="text" id="nsubs">
            </label>

        </div>


        <script>
           $.ajax({
                type: 'get',
                url: '/projects-userinfo-json/{{user.id_client}}',
                dataType: 'json',

                success: (data) => {

                    var count = data.length;
                    $('#nprojects').val(count);
                    console.log(data);
                }

            });

            $.ajax({
                type: 'get',
                url: '/tc-info-client/{{user.id_client}}',
                dataType: 'json',

                success: (data) => {

                    var count = data[0].total_units;
                    $('#unitsbought').val(count);
                    console.log(data);
                }

            });
  

             $.ajax({
                type: 'get',
                url: '/tcused-info-client/{{user.id_client}}',
                dataType: 'json',

                success: (data) => {

                       var count = data[0].total_units;
                    $('#unitsUsed').val(count);
                    console.log(data);
                }

            });


/*
           $.ajax({
                type: 'get',
                url: '/tcused-info-client/{{user.id_client}}',
                dataType: 'json',

                success: (data) => {

                   
                    console.log(data);
                }

            });
*/
            $.ajax({
                type: 'get',
                url: '/get-client-subscription/{{user.id_client}}',
                dataType: 'json',

                success: (data) => {
                    var count = data.length;
                    $('#nsubs').val(count);
                    //nsubs
                    console.log(data);
                }

            });

        </script>

    </div>
</div>

{{else}}

<div class="row">
    <div class="col-lg-12">
         <h3>Usted no puede solicitar revisión de condiciones si un id de cliente</h3>

    </div>

{{/if}}